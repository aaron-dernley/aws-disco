# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml

# Claude Code configuration (managed by swamp)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
# END swamp managed section

# Rendered diagrams embed the live estate — account id, VPC/subnet/instance ids,
# private IPs, load balancer DNS names and workload names. Regenerate them with
# the workflow; never commit them, especially to a public repo.
diagrams/

